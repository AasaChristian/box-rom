
// This is the initial state prior to fetching
const initialState = {
trailers :[],
loads : [],
materials: []   
      };


      export const reducer = (state = initialState, action) => {
        switch (action.type){
            default:
                return state;
        }

      }